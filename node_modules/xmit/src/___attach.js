


/** 
 * 	@function
 *		@description
 * 		this will embed Xmit into another class by proxying 
 * 		the commands add ( add listener ), on (on event ) and xmit
 * 		to the parent class.  
 * 	
 *		It is possible to place the xmit class into the parent class 
 * 		as an instance variable for example..  This is not strictly necessary, however.
 *	 	
 *	@param {object} object
 * 		@description : The object to place the xmit class into. 
 *	
 *	@param {object} Xmitter: 
 *		@description : The Xmit object to be proxied to the embedding object.
 */


module.exports = function(object, xmitter) { 
	object.add = function() { 
		xmitter.add.apply(this, arguments);
	},
	object.on = function() { 
		xmitter.on.apply(this, arguments);
	}
	object.xmit = function() { 
		xmitter.xmit.apply(this, arguments);
	}
}